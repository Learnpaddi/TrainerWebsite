version: '3.8'
services:
  backend:
    build: .
    ports:
      - '3001:3001'
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - redis
  redis:
    image: redis:alpine
    ports:
      - '6379:6379'
volumes:
  node_modules:
